"use server";
import {
  BookingModel,
  PortalUserSessionModel,
} from "@/app/server/getaways-shared-models/models";

const addActionToDb = async (action: string, user_id: string, booking_id: string): Promise<void> => {};
